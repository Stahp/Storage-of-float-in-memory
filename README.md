the "project.py" file uses global variable.
the "project1.py" file uses a class.

This is how float numbers are stored, added, substracted, multiplied inside the memory. ( using registers A, B, C, R and S)
